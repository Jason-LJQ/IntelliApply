# IntelliApply Configuration File
# Please replace the placeholder values with your actual credentials.

# OpenAI API Configuration
# List of API services, tried in order from top to bottom
api_services:
  - api_key: "AIxxxxxx_YGXlZBVceXfxxxnNWofOMU-xxxxx"
    base_url: "https://generativelanguage.googleapis.com/v1beta/openai/"
    model: "gemini-2.5-flash"
    reasoning_effort: "none" # Optional: none, low, medium, high
  
  # Example: Add more services as fallback
  # - api_key: "sk-proj-xxxxx"
  #   base_url: "https://api.openai.com/v1/"
  #   model: "gpt-4o"
  #   reasoning_effort: "high"

# File path configuration
paths:
  # Excel file path for tracking applications
  excel_file_path: "/path/to/your/excel/file.xlsx"

  # Folder path to store job snapshots/backups
  backup_folder_path: "/path/to/job_snapshot/folder"
